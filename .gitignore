# Rust
/target/
**/*.rs.bk
# Cargo.lock is committed for the binary workspace (reproducible CLI builds).

# Editors / OS
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Local artifacts
*.tar.gz
*.log
.direnv/
.env
.env.*

# Project-local BuildKit cache from `lamina build` (`lamina clear` removes this)
.lamina/build-cache/
